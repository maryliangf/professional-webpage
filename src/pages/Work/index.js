import React, { Component } from 'react'
import PropTypes from 'prop-types'

class WorkPage extends Component {
  static propTypes = {
    prop: PropTypes
  }

  render() {
    return (
      <div>
        To change this page go to <code>/src/pages/Work/index.js</code>
      </div>
    )
  }
}

export default WorkPage